schema: spec-driven

context: |
  Tech stack: React + TypeScript + Vite + PWA (planned baseline)
  Architecture: local-first, offline-first, single-user mobile web app
  Product focus: gym workout tracking with editable routines and historical analytics
  Testing: vitest + @testing-library/react installed and working; run with `npx vitest run`
  Style: mobile-first UI, high-contrast athletic visual system, accessible form inputs

strict_tdd: false

testing:
  detected: 2026-05-14
  strict_tdd_reason: Test runner exists but strict TDD is not enforced
  test_runner:
    available: true
    framework: vitest
    command: npx vitest run
  layers:
    unit:
      available: true
      tool: vitest
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality_tools:
    linter:
      available: true
      command: npm run lint
    type_checker:
      available: true
      command: npm run typecheck
    formatter:
      available: false
      command: null

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (foundation, domain, UX, analytics)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Keep the workout session flow fast for mobile use
  verify:
    - Run tests when infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas
